services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data/stories:/app/data/stories
      - ./data/telemetry:/app/data/telemetry
    restart: unless-stopped
